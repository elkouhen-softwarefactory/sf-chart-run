repositories:
  - name: softwarefactory
    url: https://elkouhen.github.io/sf-charts/

releases:

  - name: nexus
    namespace: prod
    chart: softwarefactory/nexus-impl
    values:
      - "./values.yaml"